import pandas as pd

# you can use pandas to read the manifest
# the manifest structure is container, weight, name

# pd.read_csv(filename, delimiter = ', ', columns = ['Position', 'Weight', 'Name'] should return a dataframe, then we just have to convert it to a matrix
